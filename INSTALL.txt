To test the modules, run `make build` and then in utop `#use` either "player.ml", "state.ml", or "deck.ml".

You can then test their functions.

To play the game, run `make play` and follow the on-screen prompts.

`make clean` deletes the old build file. 